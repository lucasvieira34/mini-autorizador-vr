server:
  port: 8080

spring:
  application:
    name: @project.name@
  datasource:
    url: jdbc:mysql://localhost:3306/miniautorizador
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
  security:
    user:
      name: username
      password: password